# セキュリティチェックリスト

**プロジェクト名**:
**実施者**:
**実施日**:
**確認者**:

---

> このチェックリストはGitHub push前・本番投入前に必ず実施してください。
> 1つでも「×」があれば本番投入禁止です。

---

## A. 認証情報・シークレット管理

| # | チェック項目 | 判定 | 備考 |
|---|---|---|---|
| A-1 | APIキーをコードにハードコードしていない | ○ / × / N/A | |
| A-2 | パスワードをコードにハードコードしていない | ○ / × / N/A | |
| A-3 | トークン（OAuth・JWT等）をコードにハードコードしていない | ○ / × / N/A | |
| A-4 | スプレッドシートIDなどの固有IDを.envで管理している | ○ / × / N/A | |
| A-5 | .envファイルを使って秘密情報を管理している | ○ / × / N/A | |
| A-6 | .env.exampleに実際の値を書いていない（プレースホルダーのみ） | ○ / × / N/A | |
| A-7 | Bitwardenに認証情報を登録済みである | ○ / × / N/A | |

---

## B. Git・GitHubの設定

| # | チェック項目 | 判定 | 備考 |
|---|---|---|---|
| B-1 | .gitignoreに `.env` が含まれている | ○ / × / N/A | |
| B-2 | .gitignoreに `*.settings.local.json` が含まれている | ○ / × / N/A | |
| B-3 | .gitignoreに `logs/` が含まれている（またはログファイルを除外している） | ○ / × / N/A | |
| B-4 | .gitignoreに `backup/` が含まれている | ○ / × / N/A | |
| B-5 | `git diff` でコミット予定ファイルに秘密情報がないか確認した | ○ / × / N/A | |
| B-6 | `git log` で過去コミットに秘密情報が含まれていないか確認した | ○ / × / N/A | |
| B-7 | GitHubリポジトリのPublic / Private設定を確認した | ○ / × / N/A | |

> ⚠ B-5 確認方法:
> ```bash
> git diff --cached
> git grep -r "password\|api_key\|secret\|token" -- "*.py" "*.js" "*.gs"
> ```

---

## C. ログ・出力の安全性

| # | チェック項目 | 判定 | 備考 |
|---|---|---|---|
| C-1 | ログにAPIキーが出力されない | ○ / × / N/A | |
| C-2 | ログにパスワードが出力されない | ○ / × / N/A | |
| C-3 | ログに個人情報をフルで出力していない（最小限のID等のみ） | ○ / × / N/A | |
| C-4 | ログファイルのアクセス権限が適切である | ○ / × / N/A | |
| C-5 | ログファイルのローテーションと保存期間が設定されている | ○ / × / N/A | |

---

## D. データの取り扱い

| # | チェック項目 | 判定 | 備考 |
|---|---|---|---|
| D-1 | rawデータ（元データ）を直接変更していない | ○ / × / N/A | |
| D-2 | 処理前にバックアップを取っている | ○ / × / N/A | |
| D-3 | バックアップファイルをGitHubにpushしていない | ○ / × / N/A | |
| D-4 | CSVや帳票に含まれる個人情報の保管場所を把握している | ○ / × / N/A | |
| D-5 | 個人情報を最小限しか保存していない（不要な情報を取得しない） | ○ / × / N/A | |
| D-6 | テスト用データに本番の個人情報を使っていない | ○ / × / N/A | |

---

## E. 外部APIの利用

| # | チェック項目 | 判定 | 備考 |
|---|---|---|---|
| E-1 | 利用するAPIの利用規約を確認した | ○ / × / N/A | |
| E-2 | APIのレートリミット（リクエスト上限）を把握している | ○ / × / N/A | |
| E-3 | APIの権限スコープを最小限に設定している | ○ / × / N/A | |
| E-4 | APIキーの有効期限を把握している | ○ / × / N/A | |
| E-5 | APIキーが漏洩した場合の無効化手順を把握している | ○ / × / N/A | |

---

## F. アクセス権限

| # | チェック項目 | 判定 | 備考 |
|---|---|---|---|
| F-1 | 実行ユーザーに必要以上の権限を与えていない（最小権限の原則） | ○ / × / N/A | |
| F-2 | スプレッドシート・フォルダ等の共有設定が適切である | ○ / × / N/A | |
| F-3 | 退職者・異動者のアクセス権が整理されている | ○ / × / N/A | |
| F-4 | 担当者変更時の権限引き継ぎ手順が文書化されている | ○ / × / N/A | |

---

## G. 担当者変更・退職時のチェック（発生時のみ）

| # | チェック項目 | 判定 | 備考 |
|---|---|---|---|
| G-1 | 退職者のAPIキーを無効化・削除した | ○ / × / N/A | |
| G-2 | 退職者のGitHubアクセスを削除した | ○ / × / N/A | |
| G-3 | 退職者のGoogleアカウントによるAPI認証を別アカウントに移行した | ○ / × / N/A | |
| G-4 | 新担当者がBitwardenから認証情報を取得できる状態か確認した | ○ / × / N/A | |
| G-5 | 新担当者がREADMEと運用手順書だけでシステムを起動できるか確認した | ○ / × / N/A | |

---

## 判定基準

| 判定 | 対応 |
|---|---|
| 全項目 ○ または N/A | 本番投入OK |
| × が1件以上 | 対応完了後に再チェック。対応するまで本番投入禁止 |

---

## セキュリティインシデント発生時の連絡先

| 種別 | 連絡先 | 対応内容 |
|---|---|---|
| APIキー漏洩 | | 即時無効化→Bitwarden更新→影響範囲確認 |
| 個人情報漏洩 | | 関係者報告→事実関係確認 |

---

## 実施サイン

| 役割 | 氏名 | 日付 |
|---|---|---|
| 実施者 | | |
| 確認者 | | |

---

*SMR-Lab セキュリティチェックリスト — v1.0.0*
